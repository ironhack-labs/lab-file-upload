<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Welcome to IronTumblr</h1>
    {{#each posts }}
        <h3>{{this.content}}</h3>
        <h4>Create by: <a href="/profilePage/{{this.creatorId._id}}">{{this.creatorId.username}}</a></h4>
        <img src="{{this.picPath}}" alt="image_1">
        <h4>{{this.picName}}</h4>

        <form action="/comment/{{id}}" method="post" enctype="multipart/form-data">
        <div class="form-group">
          <label for="content">Comment</label>
          <input type="text" name="content" class="form-control">
        </div>
        <div class="form-group">
          <label for="picPath">Comment Photo</label>
          <input type="file" name="picPath" class="form-control">
        </div>
        <div class="form-group">
          <label for="picName">Comment Photo Name</label>
          <input type="text" name="picName" class="form-control">
        </div>
        <button type="submit">Comment</button>
      </form>
    {{/each}}
  </body>
</html>
